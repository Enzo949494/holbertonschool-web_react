// Define type RowID as number
export type RowID = number;

// Create interface RowElement with required fields
export interface RowElement {
  firstName: string;
  lastName: string;
  age?: number; // Optional field
}
